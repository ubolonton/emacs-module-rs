[package]
name = "emacs"
version = "0.1.0"
authors = ["Aaron France <aaron.l.france@gmail.com"]
description = "Rust library for creating emacs modules"
license = "BSD-3-Clause"

[dependencies]
libc = "0.2"

[lib]

name = "emacs"
path = "src/lib.rs"
